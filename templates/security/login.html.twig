{% extends "base.html.twig" %}

{% import "form.html.twig" as form %}

{% block head %}<title>Blog Professionnel - Connexion</title>{% endblock %}

{% block content %}
    <div id="base-form-page">
        <header>
            <div class="container">
                <h1>Connexion</h1>
            </div>
        </header>
        <section>
            <div id="login-structure">   
                {% if errors|length > 0 %}
                <ul>

                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                    
                </ul>
                {% endif %}
                <form class="container" method="post">
                <input type="hidden" name="_csrf_token" id="_csrf_token" value={{ session.token }} />
                    <!-- <div class="row mb-3">
                        <label for="exampleInputEmail1" class="form-label">E-mail</label>
                        <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="example@mail.com">
                    </div>
                    <div class="row mb-3">
                        <label for="exampleInputPassword1" class="form-label">Mot de passe</label>
                        <input type="password" name="password" class="form-control" placeholder="Entrer le mot de passe" id="password">
                        <button type="button" class="btn btn-primary" id="showpassword">Voir</button>
                    </div> -->
                    
                    {{ form.input('email', 'Identifiant', email, {type: 'email'}) }}
                    {{ form.inputPassword('password', 'Mot de passe', password) }}

                    <div class="row mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="exampleCheck1">
                        <label class="form-check-label" for="exampleCheck1">Se souvenir du mot de passe</label>
                    </div>
                    <div class="row" id="base-form-button-row">
                        <button type="submit" class="btn btn-primary" id="base-form-button">Se connecter</button>
                    </div>
                </form>
            </div>
        </section>
    </div>
{% endblock %}

<!-- Script Js Show/Hide password -->
{% include 'parts/_scriptJs.html.twig' %}