{% extends "base.html.twig" %}

{% block head %}
    <title>Blog Professionnel - Articles</title>
{% endblock %}

{% block content %}
    <div class="container">
        <h2>Les derniers articles</h2>
    </div>
    

    
    <div class="container">
        <div class="row">
            {% for article in articles %}
                <div class="col-6">
                    <div class="card">
                    <img src="img/php_img.png" class="card-img-top" alt="Php image">
                        <div class="card-body">
                            <h5 class="card-title">{{ article.title }}</h4>
                            <h6 class="card-subtitle mb-2 text-muted">{{ article.leadParagraph }}</h5>
                            <p class="card-text">{{ article.content }}</p>
                        </div>
                        <div class="card-footer text-muted">
                                2 days ago
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

    

{% endblock %}