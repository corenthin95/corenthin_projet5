{% extends "base.html.twig" %}

{% block head %}
    <title>Blog Professionnel - {{ article.title }}</title>
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card">
                <img src="/img/php_img.png" class="card-img-top" alt="Php image">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.title }}</h4>
                        <h6 class="card-subtitle mb-2 text-muted">{{ article.leadParagraph }}</h5>
                        <p class="card-text">{{ article.content }}</p>
                    </div>
                    <div class="card-footer"><small class="text-muted">{{ article.lastUpdate | date("d/m/Y H:i") }}</small></div>
                </div>
            </div>
        </div>
        <div class="row">
            {% for comment in comments %}
            <div class="col-12">
                {{ comment.content }}
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
